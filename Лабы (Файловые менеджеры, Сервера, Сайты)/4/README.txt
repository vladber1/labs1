Пример клиент-серверного приложения, использующего многопоточность для обработки соединений с клиентами. 
Каждый клиент обрабатывается в отдельном потоке, что позволяет работать с несколькими клиентами одновременно. 

Программа работает в командной строке и управляется через ввод с клавиатуры.